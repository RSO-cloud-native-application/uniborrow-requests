kumuluzee:
  name: requests-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8081
    http:
      port: 8081
  discovery:
    consul:
      hosts: http://localhost:8500
    ttl: 30
    ping-interval: 5
  datasources:
    - jndi-name: jdbc/RequestsDB
      connection-url: jdbc:postgresql://localhost:5431/requests
      username: dbuser
      password: postgres
  health:
    checks:
      data-source-health-check:
        type: liveness
        jndi-name: jdbc/RequestsDB
    metrics:
      web-instrumentation:
        - name: requests-endpoint
          url-pattern: /v1/requests/*
rest-properties:
  maintenance-mode: false
  broken: false